[tool.poetry]
name = "project_template"
version = "0.1.0"
description = "テンプレートプロジェクト"
authors = ["TITManagement <info@titmanagement.com>"]

[tool.poetry.dependencies]
python = ">=3.8,<4.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
